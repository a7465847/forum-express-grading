<div class="d-flex align-items-center">
  <h1>餐廳後台</h1>
  <a href='/admin/users' class="btn btn-warning ml-2 mr-2">Users</a> /
  <a href='/admin' class="btn btn-warning ml-2">Restaurants</a>
</div>

<a href='/admin/restaurants/create'>
  <button type="button" class="btn btn-primary">New Restaurant</button>
</a>

<br />
<br />

<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th scope="col">Email</th>
      <th scope="col">Role</th>
      <th scope="col">#</th>
    </tr>
  </thead>
   {{#each users}}
      <tr>
        <th scope="row">{{this.id}}</th>
        <td>{{this.name}}</td>
        <td>{{this.email}}</td>
        <td>
          {{#if this.isAdmin}}
            admin
          {{else}}
            user
          {{/if}}
        </td>
        <td>
          <form action="/admin/users/{{this.id}}/toggleAdmin?_method=PUT" method="POST" style="display: inline;">
            <button type="submit" class="btn btn-link">
              {{#if this.isAdmin}}
                Set as user
              {{else}}
                Set as admin
              {{/if}}
            </button>
          </form>
        </td>
      </tr>
    {{/each}}
  
  <tbody>
 
  </tbody>
</table>